data:
  speech_h5_path: '${COCHDNN_JSIN_TRAIN_H5}'
  noise_h5_path: '${COCHDNN_AUDIONOISE_TRAIN_H5}' 
  val_speech_h5_path: '${COCHDNN_JSIN_VALID_H5}'
  val_noise_h5_path: '${COCHDNN_AUDIONOISE_VALID_H5}' 
  dataset: MatchedSpeechInNoiseDatasetBatched
  target_keys: ['signal/word_int', 'signal/speaker_int', 'noise/labels_int']

audio_rep:                                  # Attributes of audio feature
  name: 'cochleagram_1'
  on_gpu: True

audio_transforms:
  low_snr: -10
  high_snr: 10
  dbspl: 60

val_metric:
    word_task: 'val_signal/word_int_acc'
    speaker_task: 'val_signal/speaker_int_acc'
    audioset_task: 'val_noise/labels_int_acc'

hparas:                                   # Experiment hyper-parameters
  epochs: 100                              
  batch_size: 48                          # using Matched dataset - true global batch size is 4x this (e.g. 768 if 192)
  global_batch_size: 192                  # Use true global batch size for LARS initial LR calc 
  optimizer: "LARS"
  num_warmup_steps_or_ratio: 0.01
  lr: 0.2                                 # base LR for LARS
  valid_step: 5000
  task_loss_params:
    signal/word_int:
      loss_type: 'crossentropyloss'
      weight: 1.0                         # init loss is ~6.6 
    noise/labels_int:
      loss_type: 'bcewithlogitsloss'
      weight: 300.0                        # init loss is ~200 
    signal/speaker_int:
      loss_type: 'crossentropyloss'
      weight: 0.25                        # init loss is ~1.65

model:                                    # Model architecture
  arch_name: "resnet_multi_task18"
  arch_params:
    num_classes:
      signal/word_int: 794               # TODO: -1 from this if not using null
      signal/speaker_int: 433            # TODO: -1 from this if not using null
      noise/labels_int: 517              # TODO: -1 from this if not using null
    pretrained: False